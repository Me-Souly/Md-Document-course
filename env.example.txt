# Скопируй этот файл в .env и заполни значениями
# 
# ВАЖНО: Пароли и секреты нужно придумать самому!
# Это твои настройки для проекта.

# ============================================
# ОБЩИЕ НАСТРОЙКИ (можно оставить как есть)
# ============================================
NODE_ENV=production
PORT=5000
CLIENT_PORT=3000

# ============================================
# MONGODB CONFIGURATION
# ============================================
# Придумай пароль для MongoDB администратора
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=ПРИДУМАЙ_ПАРОЛЬ_САМ
MONGO_DATABASE=notes_db

# ============================================
# REDIS CONFIGURATION
# ============================================
# Придумай пароль для Redis (или оставь пустым)
REDIS_PASSWORD=ПРИДУМАЙ_ПАРОЛЬ_САМ

# ============================================
# DATABASE URL (формируется автоматически из MONGO_* переменных)
# Или укажи вручную:
# ============================================
DB_URL=mongodb://admin:your_secure_password_here@mongodb:27017/notes_db?authSource=admin

# ============================================
# JWT CONFIGURATION
# ============================================
# ОБЯЗАТЕЛЬНО! Придумай случайные строки минимум 32 символа
# Можно сгенерировать: openssl rand -base64 32
JWT_ACCESS_SECRET=ПРИДУМАЙ_СЛУЧАЙНУЮ_СТРОКУ_32_СИМВОЛА
JWT_REFRESH_SECRET=ПРИДУМАЙ_СЛУЧАЙНУЮ_СТРОКУ_32_СИМВОЛА
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
JWT_REFRESH_EXPIRES_DAYS=7
RESET_TOKEN_EXPIRES_MINUTES=60
ACTIVATION_TOKEN_EXPIRES_DAYS=7

# ============================================
# CORS & API URL
# ============================================
CLIENT_URL=http://localhost:3000
API_URL=http://localhost:5000

# ============================================
# SMTP CONFIGURATION (для активации и восстановления пароля)
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_email_password

# ============================================
# MODERATOR CONFIGURATION (создается при первом запуске)
# ============================================
# Опционально: придумай учетные данные модератора
MODERATOR_EMAIL=moderator@example.com
MODERATOR_LOGIN=moderator
MODERATOR_PASSWORD=ПРИДУМАЙ_ПАРОЛЬ_МОДЕРАТОРА

# ============================================
# FRONTEND CONFIGURATION (для клиента)
# ============================================
# Эти переменные встраиваются в JavaScript код при сборке
REACT_APP_WS_URL=ws://localhost:5000
# REACT_APP_API_URL опционально - код автоматически определит из window.location
# REACT_APP_API_URL=http://localhost:5000/api

