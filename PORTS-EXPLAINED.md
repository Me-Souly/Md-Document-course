# Что такое PORT и CLIENT_PORT (строки 10-12)?

## 📋 Строки 10-12 в .env:

```bash
PORT=5000              # Порт для сервера (Backend)
CLIENT_PORT=3000       # Порт для клиента (Frontend)
```

## 🎯 Что это такое?

Это **порты на твоем компьютере**, на которые Docker прокидывает контейнеры.

### PORT=5000 (строка 10)

**Порт для Backend сервера (Node.js)**

```yaml
# docker-compose.yml
server:
  ports:
    - "${PORT:-5000}:5000"  # ← твой компьютер:5000 → контейнер:5000
```

**Что это значит:**
- Когда ты открываешь `http://localhost:5000` → запрос идет в контейнер `server`
- Сервер внутри контейнера слушает на порту `5000`
- Docker прокидывает этот порт на твой компьютер

**Откуда брать:** Можешь оставить `5000` или изменить на другой порт (например, `8080`)

**Пример:**
```bash
PORT=5000    # Сервер будет доступен на http://localhost:5000
# или
PORT=8080    # Сервер будет доступен на http://localhost:8080
```

---

### CLIENT_PORT=3000 (строка 11)

**Порт для Frontend клиента (React + nginx)**

```yaml
# docker-compose.yml
client:
  ports:
    - "${CLIENT_PORT:-3000}:80"  # ← твой компьютер:3000 → контейнер:80
```

**Что это значит:**
- Когда ты открываешь `http://localhost:3000` → запрос идет в контейнер `client`
- Nginx внутри контейнера слушает на порту `80`
- Docker прокидывает этот порт на твой компьютер как `3000`

**Откуда брать:** Можешь оставить `3000` или изменить на другой порт (например, `3001`)

**Пример:**
```bash
CLIENT_PORT=3000    # Клиент будет доступен на http://localhost:3000
# или
CLIENT_PORT=3001    # Клиент будет доступен на http://localhost:3001
```

---

## 🔍 Как это работает:

```
┌─────────────────────────────────────────┐
│  Твой компьютер                        │
│                                         │
│  http://localhost:3000  →  Docker      │
│  http://localhost:5000  →  Docker     │
└─────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────┐
│  Docker контейнеры                       │
│                                         │
│  client:80  (nginx)                    │
│  server:5000 (Node.js)                  │
└─────────────────────────────────────────┘
```

## ✅ Что делать?

**Просто оставь как есть:**
```bash
PORT=5000
CLIENT_PORT=3000
```

**Или измени, если порты заняты:**
```bash
PORT=8080        # Если 5000 занят
CLIENT_PORT=3001 # Если 3000 занят
```

## 🐛 Как проверить, занят ли порт?

**Windows:**
```bash
netstat -ano | findstr :5000
netstat -ano | findstr :3000
```

**Linux/Mac:**
```bash
lsof -i :5000
lsof -i :3000
```

Если порт занят - измени значение в `.env`.

## 🎯 Итого

- `PORT=5000` - порт для Backend сервера
- `CLIENT_PORT=3000` - порт для Frontend клиента
- **Откуда брать:** Просто укажи порты (можно оставить по умолчанию)
- **Зачем:** Чтобы Docker знал, на какие порты прокидывать контейнеры

**Просто оставь как есть - это стандартные порты!**

