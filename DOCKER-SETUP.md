# 🐳 Как упаковать проект в Docker

## 📋 Шаг 1: Создай файл `.env`

Создай файл `.env` в **корне проекта** (рядом с `docker-compose.yml`), скопировав из `.env.example`:

```bash
cp .env.example .env
```

Или создай вручную с такими переменными:

```bash
# ============================================
# ОБЩИЕ НАСТРОЙКИ
# ============================================
NODE_ENV=production
PORT=5000
CLIENT_PORT=3000

# ============================================
# MONGODB
# ============================================
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=your_secure_password_here
MONGO_DATABASE=notes_db
DB_URL=mongodb://admin:your_secure_password_here@mongodb:27017/notes_db?authSource=admin

# ============================================
# REDIS
# ============================================
REDIS_PASSWORD=your_redis_password_here

# ============================================
# JWT
# ============================================
JWT_ACCESS_SECRET=your_jwt_access_secret_minimum_32_characters
JWT_REFRESH_SECRET=your_jwt_refresh_secret_minimum_32_characters
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
JWT_REFRESH_EXPIRES_DAYS=7
RESET_TOKEN_EXPIRES_MINUTES=60
ACTIVATION_TOKEN_EXPIRES_DAYS=7

# ============================================
# CORS & API
# ============================================
CLIENT_URL=http://localhost:3000
API_URL=http://localhost:5000

# ============================================
# SMTP (для активации и восстановления пароля)
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_email_password

# ============================================
# MODERATOR (создается при первом запуске)
# ============================================
MODERATOR_EMAIL=moderator@example.com
MODERATOR_LOGIN=moderator
MODERATOR_PASSWORD=your_moderator_password

# ============================================
# FRONTEND (опционально)
# ============================================
REACT_APP_WS_URL=ws://localhost:5000
# REACT_APP_API_URL - не задавай, код определит автоматически
```

**Важно:** 
- `JWT_ACCESS_SECRET` и `JWT_REFRESH_SECRET` должны быть минимум 32 символа
- Используй сильные пароли для MongoDB, Redis и модератора

## 🚀 Шаг 2: Собери и запусти Docker контейнеры

### Вариант A: Первый запуск (с пересборкой)

```bash
docker-compose up -d --build
```

### Вариант B: Обычный запуск (если уже собрано)

```bash
docker-compose up -d
```

### Что происходит:

1. **Собираются образы:**
   - `client` - React приложение через rspack → nginx
   - `server` - Node.js сервер

2. **Запускаются контейнеры:**
   - `mongodb` - база данных
   - `redis` - кэш
   - `server` - бэкенд API
   - `client` - фронтенд

3. **Создаются volumes** (для хранения данных MongoDB и Redis)

## ✅ Шаг 3: Проверь что всё работает

### Проверь статус контейнеров:

```bash
docker-compose ps
```

Все должны быть в статусе `Up` или `healthy`.

### Проверь логи:

```bash
# Все логи
docker-compose logs

# Логи конкретного сервиса
docker-compose logs server
docker-compose logs client
docker-compose logs mongodb
docker-compose logs redis

# Логи в реальном времени
docker-compose logs -f server
```

### Открой приложение:

- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:5000/api
- **Health Check:** http://localhost:5000/api/health

## 🔧 Полезные команды

### Остановка:

```bash
# Остановить контейнеры
docker-compose stop

# Остановить и удалить контейнеры
docker-compose down

# Остановить и удалить контейнеры + volumes (удалит данные!)
docker-compose down -v
```

### Пересборка:

```bash
# Пересобрать все сервисы
docker-compose build

# Пересобрать конкретный сервис
docker-compose build client
docker-compose build server

# Пересобрать без кэша
docker-compose build --no-cache
```

### Перезапуск:

```bash
# Перезапустить все сервисы
docker-compose restart

# Перезапустить конкретный сервис
docker-compose restart server
```

## 🐛 Решение проблем

### Проблема: Контейнеры не запускаются

```bash
# Проверь логи
docker-compose logs

# Проверь что порты свободны
netstat -ano | findstr :3000
netstat -ano | findstr :5000
```

### Проблема: Ошибки при сборке

```bash
# Пересобери без кэша
docker-compose build --no-cache

# Удали старые образы
docker-compose down
docker system prune -a
```

### Проблема: MongoDB не подключается

```bash
# Проверь логи MongoDB
docker-compose logs mongodb

# Проверь переменные окружения
docker exec note-editor-server env | grep DB_URL
```

### Проблема: Redis не подключается

```bash
# Проверь логи Redis
docker-compose logs redis

# Проверь подключение
docker exec -it note-editor-redis redis-cli ping
```

## 📦 Что упаковывается в Docker

### Клиент (client):
- ✅ Исходный код React (`src/`)
- ✅ Конфигурация (`package.json`, `rspack.config.js`, `tsconfig.json`)
- ✅ Публичные файлы (`public/`)
- ❌ `node_modules` - устанавливается заново
- ❌ `build` - собирается заново

### Сервер (server):
- ✅ Весь исходный код (`controllers/`, `services/`, `models/`, и т.д.)
- ✅ Конфигурация (`package.json`)
- ❌ `node_modules` - устанавливается заново
- ❌ `.env` - использует переменные из `docker-compose.yml`

### Базы данных:
- ✅ MongoDB - образ `mongo:7.0`
- ✅ Redis - образ `redis:7-alpine`
- ✅ Данные хранятся в Docker volumes

## 🌐 Доступ с других устройств

Если хочешь открыть приложение с телефона или другого ПК в локальной сети:

1. **Узнай IP своего компьютера:**
   ```bash
   # Windows
   ipconfig
   # Ищи "IPv4 Address" (например, 192.168.1.100)
   ```

2. **Открой приложение по IP:**
   ```
   http://192.168.1.100:3000
   ```

3. **Код автоматически определит IP** и будет использовать его для API.

## 🎯 Итого

1. ✅ Создай `.env` файл (скопируй из `.env.example`)
2. ✅ Запусти `docker-compose up -d --build`
3. ✅ Открой http://localhost:3000
4. ✅ Готово!

**Всё упаковано и работает!** 🎉
