# Откуда брать переменные окружения?

## 🎯 Главное правило

**Большинство переменных нужно придумать самому** - это пароли, секреты и настройки для твоего проекта.

## 📋 Разбор переменных по категориям

### 1️⃣ ОБЩИЕ НАСТРОЙКИ (строки 5-8)

```bash
NODE_ENV=production    # Окружение: production или development
PORT=5000              # Порт сервера (можно оставить 5000)
CLIENT_PORT=3000       # Порт клиента (можно оставить 3000)
```

**Откуда брать:** Просто укажи значения. Можно оставить как есть.

---

### 2️⃣ MONGODB (строки 11-15)

```bash
MONGO_ROOT_USERNAME=admin                    # Имя администратора MongoDB
MONGO_ROOT_PASSWORD=your_secure_password    # Пароль администратора
MONGO_DATABASE=notes_db                      # Имя базы данных
```

**Откуда брать:**
- `MONGO_ROOT_USERNAME` - можешь оставить `admin` или придумать своё
- `MONGO_ROOT_PASSWORD` - **придумай сам** (сильный пароль, минимум 8 символов)
- `MONGO_DATABASE` - можешь оставить `notes_db` или придумать своё

**Пример:**
```bash
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=MySecurePass123!
MONGO_DATABASE=notes_db
```

---

### 3️⃣ REDIS (строки 18-20)

```bash
REDIS_PASSWORD=your_redis_password_here
```

**Откуда брать:** **Придумай сам** (сильный пароль)

**Пример:**
```bash
REDIS_PASSWORD=RedisSecurePass456!
```

**Важно:** Если не хочешь использовать пароль для Redis, можешь оставить пустым:
```bash
REDIS_PASSWORD=
```

---

### 4️⃣ JWT (строки 23-29)

```bash
JWT_ACCESS_SECRET=your_jwt_access_secret_minimum_32_characters
JWT_REFRESH_SECRET=your_jwt_refresh_secret_minimum_32_characters
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
JWT_REFRESH_EXPIRES_DAYS=7
RESET_TOKEN_EXPIRES_MINUTES=60
ACTIVATION_TOKEN_EXPIRES_DAYS=7
```

**Откуда брать:**
- `JWT_ACCESS_SECRET` - **придумай сам** (случайная строка, минимум 32 символа)
- `JWT_REFRESH_SECRET` - **придумай сам** (случайная строка, минимум 32 символа)
- Остальные - можешь оставить как есть

**Как сгенерировать секреты:**

**Windows (PowerShell):**
```powershell
# Генерирует случайную строку из 32 символов
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
```

**Linux/Mac:**
```bash
# Генерирует случайную строку из 32 символов
openssl rand -base64 32
```

**Или просто придумай длинную случайную строку:**
```bash
JWT_ACCESS_SECRET=MySuperSecretKey12345678901234567890
JWT_REFRESH_SECRET=MyRefreshSecretKey98765432109876543210
```

---

### 5️⃣ CORS & API (строки 32-34)

```bash
CLIENT_URL=http://localhost:3000
API_URL=http://localhost:5000
```

**Откуда брать:** Можешь оставить как есть для локальной разработки.

---

### 6️⃣ SMTP (строки 37-41) - ОПЦИОНАЛЬНО

```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_email_password
```

**Откуда брать:**
- Если используешь Gmail:
  - `SMTP_HOST=smtp.gmail.com`
  - `SMTP_PORT=587`
  - `SMTP_USER` - твой email
  - `SMTP_PASSWORD` - пароль приложения Gmail (не обычный пароль!)

**Важно:** Если не используешь email (активация, восстановление пароля), можешь оставить пустым:
```bash
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
```

---

### 7️⃣ MODERATOR (строки 44-47) - ОПЦИОНАЛЬНО

```bash
MODERATOR_EMAIL=moderator@example.com
MODERATOR_LOGIN=moderator
MODERATOR_PASSWORD=your_moderator_password
```

**Откуда брать:** **Придумай сам** - это учетные данные модератора, который создается при первом запуске.

**Пример:**
```bash
MODERATOR_EMAIL=admin@myapp.com
MODERATOR_LOGIN=admin
MODERATOR_PASSWORD=AdminPass123!
```

---

### 8️⃣ FRONTEND (строки 50-53)

```bash
REACT_APP_WS_URL=ws://localhost:5000
```

**Откуда брать:** Можешь оставить как есть для локальной разработки.

---

## ✅ Минимальная конфигурация (обязательные переменные)

Если хочешь быстро запустить, заполни только это:

```bash
# MongoDB
MONGO_ROOT_PASSWORD=MyMongoPass123!

# Redis (можно оставить пустым)
REDIS_PASSWORD=

# JWT (ОБЯЗАТЕЛЬНО!)
JWT_ACCESS_SECRET=MyAccessSecretKey12345678901234567890
JWT_REFRESH_SECRET=MyRefreshSecretKey98765432109876543210

# Остальное можно оставить по умолчанию
```

---

## 🎯 Итого

**Откуда брать переменные:**

1. **Пароли и секреты** → придумай сам (сильные пароли)
2. **JWT секреты** → сгенерируй случайные строки (минимум 32 символа)
3. **Порты и URLs** → можешь оставить по умолчанию
4. **SMTP и Moderator** → опционально, заполни если используешь

**Просто придумай пароли и секреты - это твои настройки для проекта!**

